{
  "name": "mean-liquid",
  "version": "0.0.1-alpha.2",
  "description": "Helps developers track, version, and deploy database changes.",
  "scripts": {
    "alpha": "npm version prerelease --preid=alpha && npm run publish-internal --tag=alpha",
    "beta": "npm version prerelease --preid=beta && npm run publish-internal --tag=beta",
    "clean": "npm unlink mean-liquid && npm uninstall -g mean-liquid && npm uninstall mean-liquid && rm -rf node_modules && npm i",
    "major": "npm version major && npm run publish-internal",
    "minor": "npm version minor && npm run publish-internal",
    "patch": "npm version patch && npm run publish-internal",
    "prerelease": "npm version prerelease && npm run publish-internal --tag=prerelease",
    "publish-internal": "npm publish --access=public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zeiglerd/mean-liquid.git"
  },
  "keywords": [
    "node",
    "js",
    "database",
    "automation",
    "source",
    "control",
    "mongodb",
    "migration"
  ],
  "author": "Dustin Zeigler",
  "license": "ISC",
  "dependencies": {
    "command-line-args": "^6.0.0",
    "dotenv": "^16.4.5",
    "mongoose": "^8.6.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  },
  "devDependencies": {
    "@types/command-line-args": "^5.2.3"
  },
  "bin": {
    "db-create": "ts-node src/scripts/create.ts",
    "db-down": "ts-node src/scripts/down.ts",
    "db-status": "ts-node src/scripts/status.ts",
    "db-up": "ts-node src/scripts/up.ts"
  }
}
